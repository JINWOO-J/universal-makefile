REPO_HUB=jinwoo
NAME=universal-makefile
VERSION = v1.0.237
# 이 repo의 main/develop 이력은 no-ff 머지 기반 — linear promote는 push 거부로 실패함
RELEASE_MERGE_STYLE = merge
IGNORE_TAG_RE = cache
DOCKERFILE_CONTEXT = .
DOCKERFILE_PATH = Dockerfile
